diff --git a/dist/src/plugins/gateway/GatewayPlugin.js b/dist/src/plugins/gateway/GatewayPlugin.js
index c03ce2be0cb3e1772684e72f3ed56f9175e8d5e2..6111d825e2a1f986a66d6f6207c2d135d9fae108 100644
--- a/dist/src/plugins/gateway/GatewayPlugin.js
+++ b/dist/src/plugins/gateway/GatewayPlugin.js
@@ -70,7 +70,12 @@ export class GatewayPlugin extends Plugin {
                 this.gatewayInfo = (await response.json());
             }
             catch (error) {
-                throw new Error(`Failed to get gateway information from Discord: ${error instanceof Error ? error.message : String(error)}`);
+                const errorMessage = `Failed to get gateway information from Discord: ${error instanceof Error ? error.message : String(error)}`;
+                // Emit error event to allow graceful handling instead of crashing
+                this.emitter.emit("error", new Error(errorMessage));
+                // Return early to prevent connect() from being called with invalid state
+                // The reconnect logic will handle retrying
+                return;
             }
         }
         // Set shard information on the client
